/**
 * A regex used to validate a user's username input.
 */
export const ValidUserNameChars = /^[0-9a-zA-Z]+$/;

/**
 * A regex used to validate a user's routine name input.
 */
export const ValidRoutineNameChars = /^[0-9a-zA-Z]+$/;

/**
 * A regex used to validate a user's device name input.
 */
 export const ValidDeviceNameChars = /^[0-9a-zA-Z]+$/;
